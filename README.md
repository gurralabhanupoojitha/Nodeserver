this code demonstrates how to create a simple http server in Node.js using the builtin http module.the server listens to the port number 3000 http.createServer0 creates a server if the url is / or/ home the "Home Page".If the url is /about then "About page" If the url is /contact then "Contact Page" If we type any other than "500 Server Error"
